default:
  target: dev
  outputs:
    dev:
      type: postgres
      host: elt-postgres          # Docker service name
      user: postgres
      password: password
      port: 5432
      dbname: warehouse
      schema: public